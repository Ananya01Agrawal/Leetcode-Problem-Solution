class Solution {
    public int missingNumber(int[] nums) {
        int n=nums.length;
        int givensum=0;
        int fullsum=n*(n+1)/2;
        for(int i=0;i<nums.length;i++){
             givensum+=nums[i];
        }
        return fullsum-givensum;
    }
}
